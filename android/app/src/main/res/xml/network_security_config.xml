<?xml version="1.0" encoding="utf-8"?>
<!--
  Waiter tablets talk to the POS host on the LAN over HTTP.
  There is no public TLS certificate on that machine. Restricting
  cleartext to domain names would block 192.168.x.x, so cleartext
  stays allowed; the app does not send orders to the public internet.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
