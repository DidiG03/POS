node_modules
dist
.DS_Store
*.log
pnpm-debug.log*
.env
coverage
prisma/dev.db

# TLS material must never be committed. The LAN HTTPS certificate is
# installed per-machine under <userData>/certs (see `tlsCertDir()`).
*.pem
*.key
*.p12
*.pfx
.local-certs/

# Capacitor build outputs (Xcode/Android Studio derived files).
# Note: when you start customizing native projects, you'll want to
# COMMIT ios/ and android/ instead and only ignore their build dirs.
ios/App/Pods
ios/App/build
ios/App/DerivedData
android/.gradle
android/app/build
android/build
android/local.properties
android/app/release
android/app/debug
android/captures
android/.idea
